import './App.css';

function App() {
  return (
    <>
    <nav className="navbar">
        <button onClick={() => document.getElementById("about").scrollIntoView({ behavior: "smooth" })}>About</button>
        <button onClick={() => document.getElementById("work").scrollIntoView({ behavior: "smooth" })}>Work</button>
        <button onClick={() => document.getElementById("contact").scrollIntoView({ behavior: "smooth" })}>Contact</button>
    </nav>
    <div className="page">
      <div className="section top-white" /> 
      {/* Name Ribbon */}
      <div className="section stripe">
        <div className="content">
          <img src="/vine_image.svg" alt="Decoration" className="side-image left" />
          <h1>Lauren Bradley</h1>
          <img src="/vine_image.svg" alt="Decoration" className="side-image right flipped" />
        </div>
      </div>
      <div className="section bottom-white" />
      
      {/* About Section */}
      <section id ="about" className="content-section">
        <h2>About Me</h2>
        <p>Hola, im lauren and i need a new job bc my company keeps screwing me over</p>
      </section>

      {/* Work Section */}
      <section id="work" className="content-section">
        <h2>My Work</h2>
        <p>Here is my career im really good at my job</p>
      </section>

      {/* Contact Section */}
      <section id="contact" className="content-section">
        <h2>Contact Me</h2>
        <p>Email, LinkedIn, GitHub, or a form here.</p>
      </section>
    </div>
    </>
  );
}

export default App;
